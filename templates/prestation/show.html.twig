{% extends 'base.html.twig' %}

{% block title %}Prestation
{% endblock %}

{% block body %}
	<h1>Prestation</h1>

	<div class="card m-5" style="width: 18rem;">
		<img class="card-img-top" src="{{ asset('photos/' ~ prestation.image) }}" alt="Card image cap">

		<div class="card-body">
			<h5 class="card-title">{{ prestation.name }}</h5>

			<p class="card-text">{{ prestation.description }}</p>

			<a href="{{ path('app_prise_rendez_vous' , { 'id' : prestation.id }) }}" class="btn btn-primary">Réserver</a>


		</div>
	</div>


	<a href="{{ path('app_index') }}">back to list</a>

{% endblock %}
